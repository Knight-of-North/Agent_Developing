# AI 剧本杀主持人 · 开发总结

## 项目概述

一个基于 LangGraph 的多智能体剧本杀系统。用户扮演一个嫌疑人，与多个 AI 角色（嫌疑人 + 主持人 DM）在共享状态上协作，完成"生成剧本 → 讨论 → 投票 → 揭晓真相"的完整游戏闭环。

## 分阶段开发过程

| 阶段 | 目标 | 新引入的概念 |
|---|---|---|
| **Phase 1** | 线性流程跑通（生成剧本 → DM 主持） | StateGraph、节点、边、State、`invoke` |
| **Phase 2** | AI 玩家加入 + 多轮循环 | 条件边 `add_conditional_edges`、循环、Think/Speak 双通道 |
| **Phase 3** | 投票环节 | 确定性节点（`Counter` 统计票数）、"流程控制 vs 内容生成分离" |
| **Phase 3.5** | 用户参与 | `interrupt` 人在回路、`checkpointer`、`thread_id`、`Command(resume)` |
| **Phase 4** | 防跑飞 + 交付 | 确定性校验 + 重试、"拥抱不确定性" |

## 学到的 LangGraph 核心概念

1. **图即流程**：用 `StateGraph` 把"游戏该怎么走"画成一张图，节点是"演员"，边是"调度指令"。
2. **状态驱动路由**：条件边根据状态字段（如 `phase_round`）决定下一步，而不是写死顺序。
3. **reducer**：`messages` 用 `operator.add` 追加而非覆盖，这是多轮对话不"失忆"的关键。
4. **确定性 vs LLM 节点**：该让机器算的（统计票数、校验泄露）绝不让模型猜；该让模型干的（生成剧本、角色发言）交给模型。
5. **人在回路**：`interrupt()` 暂停图、`Command(resume)` 恢复，让用户能真正"参与"而不是旁观。

## 踩过的坑（重要经验）

1. **模型名变更**：`deepseek-chat` / `deepseek-reasoner` 于 2026-07-24 退役，需改用 `deepseek-v4-flash` / `deepseek-v4-pro`。**教训：API 变更要先查证，不能凭旧知识硬答。**
2. **V4 默认 thinking 模式**：思考链吃光 `max_tokens`，导致 `content` 为空。**解法：`reasoning_effort="none"` 关思考。**
3. **给 LLM 传"要它如实转述"的数据，必须传完整数据**：只传统计摘要（票数），DM 会自己脑补投票明细（甚至编出死人投票）。
4. **用户参与要"给角色卡"**：用户不知道自己的秘密就无从演起，发言前必须看到"我是谁、藏着什么秘密"。
5. **PowerShell 与 Bash 命令差异**：`grep` vs `findstr`；`\"` 在 PowerShell 不转义。

## 答辩要点（建议讲这三点）

1. **图编排**：展示 `graph.py` 的图结构，讲清楚"条件边 + 循环 + interrupt"如何组合出完整游戏流程。
2. **多智能体 + 状态管理**：多个角色（DM、AI 玩家、用户）如何在共享 `GameState` 上协作，`reducer` 如何保证对话历史不丢失。
3. **确定性 vs LLM 分工**：为什么统计票数、防泄露校验必须用确定性逻辑，而不是让 LLM 做——这是"流程控制 vs 内容生成分离"的核心，也是从 loverGraph 借鉴的精华。
