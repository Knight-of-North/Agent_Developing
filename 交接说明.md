# AI 剧本杀主持人 · 项目交接说明

> 给接手优化的队友。读完这份文档，你就能理解项目现状，并在其基础上继续开发。

## 一、项目是什么

一个基于 **LangGraph** 的多智能体剧本杀系统。玩家（真人）扮演一个嫌疑人，与多个 AI 角色（其他嫌疑人 + 主持人 DM）在**共享状态**上协作，完成「生成剧本 → 讨论 → 投票 → 揭晓真相」的完整游戏闭环。

## 二、技术栈

- **编排**：LangGraph（StateGraph 图编排、条件边、循环、`interrupt` 人在回路、checkpointer）
- **模型**：DeepSeek V4-Flash（通过 `langchain-deepseek` 调用）
- **界面**：Streamlit（`app.py`）+ 终端 CLI（`main.py`）双入口
- **环境**：Python 3.10（conda 环境名 `py10`）

## 三、文件结构

| 文件 | 职责 |
|---|---|
| `game_state.py` | 共享状态 `GameState` 定义（所有节点共享的"舞台"） |
| `nodes.py` | 节点函数：多智能体 + 确定性节点 + 路由函数 |
| `graph.py` | 图编排：注册节点、连线、条件边、checkpointer |
| `main.py` | 终端入口（interrupt 恢复循环） |
| `app.py` | Streamlit 图形界面入口 |
| `diagnose.py` | 调试脚本（看模型原始返回） |
| `diag.py` | 网络诊断脚本 |
| `README.md` / `开发总结.md` | 项目文档 |
| `requirements.txt` / `.env.example` | 依赖 + 密钥模板 |

## 四、核心架构

### 4.1 图结构

```
START → generate_script → dm_intro ─────────┐
                                            │（条件边 route_speaker）
        human_turn（interrupt 等用户） ←────┤
        ai_player_turn（AI 发言）     ←─────┘（讨论循环）
             │
             └─ 轮满 → ai_vote → human_vote → tally → dm_reveal → END
```

### 4.2 共享状态（`GameState`，TypedDict, total=False）

关键字段：
- `theme` / `background_style`：主题 + 背景风格（背景风格有 6 个预设选项）
- `script`：剧本 dict，含 `background / suspects / clues / truth`（每个嫌疑人含 `name / secret / forbidden`）
- `current_phase` / `phase_round`：当前阶段 + 讨论轮次
- `user_role`：用户扮演的角色名（当前固定为第一个嫌疑人）
- `messages`：对话历史，`Annotated[list, operator.add]` 追加式，元素是 `{"speaker": str, "content": str}`
- `thoughts`：AI 内心戏（追加式）
- `votes` / `vote_winner` / `vote_counts`：投票结果
- `clues_pool` / `distributed_clues`：**已预留字段，但尚未使用**（线索分发功能未实现）

### 4.3 节点清单（`nodes.py`）

| 节点 | 类型 | 职责 |
|---|---|---|
| `generate_script_node` | LLM | 生成剧本 + 指定用户角色 |
| `dm_intro_node` | LLM | DM 开场介绍 |
| `ai_player_turn_node` | LLM | AI 玩家发言（think/speak 双通道 + 防泄露重试） |
| `human_turn_node` | interrupt | 轮到用户发言（暂停等输入） |
| `ai_vote_node` | LLM | AI 玩家投票 |
| `human_vote_node` | interrupt | 用户投票 |
| `tally_node` | 确定性 | 统计票数（`Counter`，不调 LLM） |
| `dm_reveal_node` | LLM | 揭晓真相 + 对比投票 |
| `route_speaker` | 路由 | 条件边：根据 `phase_round` 判断下一个发言者 |

### 4.4 人在回路（interrupt）机制

- 图用 `MemorySaver` 做 checkpointer，`thread_id`（UUID）标识会话
- 节点里 `interrupt({...})` 暂停，入口用 `Command(resume=用户输入)` 恢复
- 检测暂停：`result["__interrupt__"][0].value`

## 五、运行方法

```bash
conda activate py10
pip install -r requirements.txt

# 终端版
python main.py

# 图形界面版
streamlit run app.py    # 浏览器 http://localhost:8501
```

配置：复制 `.env.example` 为 `.env`，填入 DeepSeek key。**模型名必须用 `deepseek-v4-flash`**（`deepseek-chat` 已于 2026-07 退役）。

## 六、关键设计思想（改动前务必理解）

1. **流程控制 vs 内容生成分离**：LLM 负责"说什么"，确定性逻辑负责"算什么、合不合规"（统计票数、防泄露校验都走纯 Python）。
2. **Think/Speak 双通道**：AI 玩家内心推理（think）与公开台词（speak）分离，只展示 speak。
3. **防跑飞**：AI 发言若泄露禁忌词（`forbidden`），自动打回重说（最多 3 次）。
4. **拥抱不确定性**：用"确定性校验 + 重试"兜底 LLM 的不稳定输出。

## 七、可优化点（你们的接手方向，按价值排序）

**A. 玩法增强**
1. **线索分发（信息差）**：`clues_pool` / `distributed_clues` 字段已预留但没用。可实现"每个角色持有不同线索"，凶手/侦探持有不同信息。
2. **线索调查机制**：玩家主动调查线索（触发确定性节点分发）。
3. **用户选择角色**：当前固定扮演第一个嫌疑人（`suspects[0]`），可改为开局让用户选。
4. **平票重投**：`tally_node` 平票时取第一个，可改为平票重投。

**B. 体验优化**
5. **流式输出**：Streamlit 目前等 LLM 全部生成完才显示，可改 `st.write_stream` 流式。
6. **对话记忆增强**：AI 只拼最近 6 条消息（`messages[-6:]`），长对话会失忆，可引入总结记忆。
7. **剧本质量**：线索指向性太强导致投票过于一致（常 4:1），可在提示词里要求线索更分散、更有误导性。

**C. 稳健性**
8. **网络兜底**：已加 `timeout + max_retries + trust_env`，仍可加应用层 try/except 友好提示。
9. **JSON 容错**：`_parse_json` 解析失败兜底返回 `{"raw": text}`，下游处理可更健壮。

**D. 工程**
10. **配置外置**：`MAX_ROUNDS`、`temperature`、背景风格列表等硬编码，可外置到配置文件。
11. **自动化测试**：目前无测试，可加。

## 八、注意事项（避坑）

1. **API key 安全**：key 在 `.env`，绝不下代码、不提交 git（`.gitignore` 已排除）。
2. **模型名**：`deepseek-v4-flash`（旧名 `deepseek-chat` / `deepseek-reasoner` 已于 2026-07-24 退役）。
3. **思考模式**：`reasoning_effort="none"` 必须保留——否则 V4 默认 thinking 模式会吃光 max_tokens 导致 content 为空。
4. **interrupt 坑**：恢复时节点从头重跑，`interrupt()` 之前的代码必须幂等（无副作用）。
5. **网络**：手机热点下访问 DeepSeek 偶发 SSL 握手失败，已加 `max_retries=5` 兜底。
6. **Windows 终端**：用 `findstr` 不是 `grep`。
