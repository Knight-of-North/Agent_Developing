# AI 剧本杀主持人 · 代码质量审查报告

> 审查对象：`Agent_Developing` 仓库（LangGraph 多智能体剧本杀项目）
> 审查范围：graph.py / nodes.py / game_state.py / app.py / main.py / 诊断脚本 / requirements.txt / README.md
> 审查日期：2026-08-17

---

## 一、总体评价

这个项目作为大学生阶段的 LangGraph 作品，完成度相当高：**"LLM 负责内容生成、确定性逻辑负责流程控制与校验"的分层思想贯穿始终**，think/speak 双通道、interrupt 人在回路、禁忌词防泄露重试、线索分发的负载均衡兜底等设计都体现了对多智能体系统"拥抱不确定性"的正确理解，注释质量尤其出色——几乎每个关键决策都写清了"为什么"。主要短板集中在四个方面：一是**少数关键路径缺少异常兜底**（剧本流式生成失败会让 Streamlit 页面直接崩溃、JSON 解析失败时会把内心戏泄露到公开频道）；二是**完全没有自动化测试**，而项目里恰恰有 8 个不需要 LLM 就能测的纯函数；三是 **nodes.py 已膨胀成 710 行的"上帝模块"**，且发言者选择逻辑在路由函数和节点函数里重复实现；四是存在几处**典型的 O(n²) 字符串拼接**和 checkpoint 状态无限增长的隐患。整体属于"设计思想成熟、工程化待补课"的状态，下述问题大多能以小改动获得大收益。

---

## 二、问题清单（按严重程度排序）

| 编号 | 文件/模块 | 问题描述 | 严重等级 | 优化建议 |
|---|---|---|---|---|
| 1 | app.py 131~137 行 | 剧本流式生成只捕获 `StopIteration`。LLM 网络异常、限流、超时会在 `next(gen)` 处直接抛出，Streamlit 页面崩溃，用户看不到任何提示 | **高** | 用 `try/except Exception` 包住迭代循环，失败时 `st.error()` 提示并允许重试；详见第三节代码示例 |
| 2 | nodes.py 524 行 `ai_player_turn_node` | `speak = out.get("speak", out.get("raw", "……"))`——当 LLM 输出不是合法 JSON 时，`_parse_json` 返回 `{"raw": 全文}`，**raw 里包含 think 内心戏，会被当作公开发言展示**，破坏信息差设计 | **高** | JSON 解析失败时绝不能把 raw 当 speak。改用正则抢救 speak 字段，抢救失败就说"……"（示例见下） |
| 3 | nodes.py 451~456 行 `dm_intro_node` | prompt 里 `登场嫌疑人：{suspects}` 把完整 suspect 字典（**含 secret 和 forbidden**）传给了 DM，而 prompt 文字又声称"你也不知道具体内容"——自相矛盾，LLM 很可能在开场白里剧透秘密 | **高** | 只传名字列表：`names = [s.get("name") for s in suspects]`，prompt 改为 `登场嫌疑人：{names}` |
| 4 | 仓库根目录 | README 让用户"复制 .env.example 为 .env"，但**仓库里不存在 .env.example**，新同学按文档跑不起来 | **高** | 补建 `.env.example`（已被 .gitignore 白名单放行），内容：`DEEPSEEK_API_KEY=你的key` + `DEEPSEEK_MODEL=deepseek-v4-flash` + `DEEPSEEK_BASE_URL=https://api.deepseek.com` |
| 5 | nodes.py 521~531 行 | 防泄露重试 3 次后**仍然泄露的发言会被原样采用**（循环耗尽后直接 return speak）。重试是概率性防御，缺确定性兜底 | 中 | 重试耗尽后做确定性脱敏：`for w in forbidden: speak = speak.replace(w, "□")`，保证绝不泄露 |
| 6 | nodes.py 488 行 vs 703 行 | "当前轮到谁发言"的逻辑 `suspects[round_num % len(suspects)]` 在 `ai_player_turn_node` 和 `route_speaker` 里**各写了一遍**，两处若改一处忘另一处，路由与实际发言者就会错位 | 中 | 抽成单一事实源：`def _current_speaker(state) -> dict`，两处都调它 |
| 7 | nodes.py 626 行 `tally_node` | **平票未处理**：`Counter.most_common(1)` 在平票时按插入顺序任取一个当"得票最多"，DM 揭晓会给出误导性结论 | 中 | 检测平票：`top = counter.most_common(); max_n = top[0][1]; winners = [k for k,v in top if v==max_n]`，len>1 时把 `vote_winner` 设为"平票（A、B）"并在 dm_reveal prompt 中说明 |
| 8 | nodes.py 556~594 行 `ai_vote_node` | AI 投票只基于公开聊天记录，**没有把每个 AI 自己的私密线索喂给它**——信息差设计在投票环节崩塌，投票质量下降 | 中 | 改为逐玩家生成投票（或一次调用但在 prompt 中按玩家分节附上各自线索），让每个 AI 基于"公开记录+自己手里的线索"投票 |
| 9 | nodes.py 整体 | 710 行单文件混杂了：名字池数据、prompt 构建、JSON 解析、名字/秘密规范化、线索分发、防泄露、9 个节点函数——典型"上帝模块" | 中 | 拆分为 `names.py`（名字池+抽样）、`prompts.py`（prompt 构建）、`validators.py`（_parse_json/_enforce_names/脱敏）、`nodes.py`（纯节点逻辑）。一次性移动、不改逻辑，风险很低 |
| 10 | 全仓库 | **零测试**。`_parse_names`、`_pick_suspect_names`、`_enforce_names`、`_normalize_secret_first_person`、`_fallback_clues`、`distribute_clues_node`、`tally_node`、`route_speaker` 全是纯函数/确定性节点，不花一分钱 token 就能测 | 中 | 新建 `tests/test_logic.py`，用 pytest 覆盖上述函数（尤其"张三；李四"中文分号、平票、holder 写错这类已踩过的坑），示例见第三节 |
| 11 | game_state.py 64~65 行 | `votes: dict` 没有 reducer，导致 `human_vote_node` 要手动 `dict(state.get("votes", {}))` 拷贝合并——注释解释了，但这是用约定对抗框架，新增投票节点容易忘 | 中 | 定义合并 reducer：`def _merge_dict(a, b): return {**a, **b}`，字段改为 `votes: Annotated[dict, _merge_dict]`，human_vote_node 直接 `return {"votes": {user_role: user_vote}}` |
| 12 | nodes.py 各节点签名 | 类型注解用 `state: dict`，丢失了 GameState 的字段提示；`custom_names: list` 等裸 list 未参数化 | 低 | 签名改 `state: GameState`，裸 list/dict 改 `list[str]`、`dict[str, list[str]]`（Python 3.10 原生支持） |
| 13 | diag.py 17 行 | 打印 `api_key[:8]`——密钥前 8 位进入日志/终端历史，有泄露面 | 低 | 改为 `api_key[:4] + "****"` 或只打印长度与"是否已配置" |
| 14 | nodes.py 709~710 行 / diagnose.py | `__main__` 块直接真实调用 LLM，误跑一次就烧一次 token | 低 | 删除 nodes.py 的 `__main__` 块；diagnose.py 保留但加一句"本脚本消耗 API 额度"注释 |
| 15 | test_api.py 2 行 | 注释 `cd campus-agent` 是旧仓库残留路径，误导 | 低 | 改为实际目录名 |
| 16 | requirements.txt | diag.py 直接 `import httpx`，但 httpx 只是 langchain 的传递依赖，未显式声明——上游版本变动可能导致 ImportError | 低 | 显式加一行 `httpx` |
| 17 | nodes.py 32 行 | 全局 `httpx.Client` 从不 close；`timeout=120 × max_retries=5` 意味着单次调用最坏要等约 10 分钟，页面像卡死 | 低 | 进程级资源可接受不 close，但建议 `max_retries` 降到 2~3，并在 app.py 用 `st.spinner` 提示等待 |
| 18 | app.py 162/213 行 | `result["__interrupt__"][0].value["type"]` 链式硬取，interrupt 结构一旦变化直接 KeyError | 低 | 用 `(result.get("__interrupt__") or [{}])[0]` + `.get("value", {}).get("type")` 防御 |

### 问题 2 的修复示例（nodes.py `ai_player_turn_node`）

```python
# 现状（危险）：JSON 解析失败时 raw 全文（含 think）被公开发布
speak = out.get("speak", out.get("raw", "……"))

# 修复：解析失败时用正则抢救 speak，抢救不到就用占位符，绝不用 raw
def _extract_speak(out: dict) -> str:
    if "speak" in out:
        return out["speak"]
    raw = out.get("raw", "")
    m = re.search(r'"speak"\s*:\s*"(.*?)"(?=\s*[,}])', raw, re.S)
    return m.group(1) if m else "……（这个角色欲言又止）"
```

---

## 三、算法优化专项

### 优化点 1：流式 token 的 O(n²) 字符串拼接（收益最大）

**现状分析**：四处用 `full += token` 在循环里累积文本——
- `nodes.py:192` `_stream_full_text`
- `nodes.py:325` `generate_script_stream`
- `app.py:134` `full_text += token`（剧本生成实时展示）
- `app.py:50` `current += token.content`（DM 台词流式）

Python 字符串不可变，每次 `+=` 都要复制已有全部内容。设最终文本长度 n，总复制量是 1+2+…+n = **O(n²)**。剧本 JSON 通常 4~8KB，意味着上万次无谓的内存复制。

**优化思路**：用 list 累积、最后 `"".join()`——每次 append 是摊还 O(1)，总复杂度降为 **O(n)**。

**示例代码对比**（以 `_stream_full_text` 为例）：

```python
# 现状：O(n²)
full = ""
for chunk in llm.stream(prompt):
    token = chunk.content or ""
    if token:
        full += token
return full

# 优化：O(n)
parts = []
for chunk in llm.stream(prompt):
    if chunk.content:
        parts.append(chunk.content)
return "".join(parts)
```

**预期提升**：时间复杂度 O(n²)→O(n)；对 8KB 剧本，字符复制总量从约 3200 万次降到约 8000 次。app.py 两处同理（`current` 用 list 累积、render 时 join）。

### 优化点 2：`_run_stream` 每个 token 全量重渲染（app.py 36~40 行）

**现状分析**：`render()` 每收到一个 token 就把 `segments + current` 全量拼成 markdown 再 `placeholder.markdown()` 重写一次 DOM。DM 一段 500 字的台词会触发 500 次全量渲染，渲染成本同样是 **O(n²)**，且 Streamlit 前端会明显抖动。

**优化思路**：节流——每累积 k 个 token（或距上次渲染超过 80ms）才渲染一次。

```python
pending = 0
for chunk in ...:
    ...
    current_parts.append(token.content)
    pending += len(token.content)
    if pending >= 24:          # 每攒够 24 个字符渲染一次
        render()
        pending = 0
render()                        # 收尾
```

**预期提升**：渲染次数从 n 降到 n/24，前端流畅度显著改善；实现只加 4 行。

### 优化点 3：checkpoint 快照随对话无限增长（内存 O(m²)）

**现状分析**：`messages` 用 `operator.add` reducer 持续追加，MemorySaver 在**每个节点执行后都保存一份完整 state 快照**。设一局产生 m 条消息，第 i 个快照大小 ∝ i，总内存 ∝ 1+2+…+m = **O(m²)**。单局 30 条消息没问题，但若以后支持长对话或多局复用同一 thread，内存会悄悄膨胀。

**优化思路**：短期不用改（单局规模小）；长期两条路——① 换 `SqliteSaver`（langgraph-checkpoint-sqlite），快照落盘不占常驻内存，还顺带获得"关程序后继续玩"能力；② 讨论历史只保留最近 N 条进 state，完整记录另存。

**预期提升**：常驻内存 O(m²)→O(m)；属于"现在埋点、以后不慌"的优化。

### 优化点 4：`_MIXED_POOL` 构建的列表成员判断（nodes.py 80~83 行）

**现状分析**：`if _name not in _MIXED_POOL` 是 O(k) 线性扫描，构建过程 O(k²)。k≈80，实际耗时可忽略，但这是教科书式的低效写法。

**优化思路**：一行改完，保序去重：

```python
_MIXED_POOL = list(dict.fromkeys(n for pool in _NAME_POOLS.values() for n in pool))
```

**预期提升**：O(k²)→O(k)。价值在于养成"去重用 dict/set"的肌肉记忆。

### 优化点 5：投票环节的并行化空间（nodes.py `ai_vote_node`）

**现状分析**：现在是**一次 LLM 调用生成全体 AI 的投票**——调用次数上已经最优。但若按问题 8 的建议改成"每个 AI 结合自己的私密线索投票"，逐玩家调用会变成串行 k 次（k≈5），每次都是阻塞 IO。

**优化思路**：`ChatDeepSeek` 支持异步，用 `asyncio.gather` 并行发起 k 个 `ainvoke`，总耗时 ≈ 单次调用耗时而非 k 倍。

```python
import asyncio
async def _one_vote(name, clues, history):
    return await llm.ainvoke(build_vote_prompt(name, clues, history))
results = asyncio.run(asyncio.gather(*[_one_vote(n, c, h) for n, c, h in players]))
```

**预期提升**：投票环节墙钟时间 O(k·T)→O(T)。难度中等，建议在完成问题 8（投票喂线索）之后再做，二者天然配套。

---

## 四、创意拓展建议

### 创意 1：断点续玩——SqliteSaver 持久化存档
- **解决痛点**：MemorySaver 是纯内存的，Streamlit 一重启（或误点"再来一局"）整局进度就没了；interrupt 的"暂停"能力只在进程存活期内有效。
- **实现思路**：把 `graph.py:78` 的 `MemorySaver()` 换成 `SqliteSaver.from_conn_string("games.db")`（需 `pip install langgraph-checkpoint-sqlite`），thread_id 即存档 ID；在 app.py 侧边栏加"历史对局"列表，点击旧 thread_id 继续 `graph.stream`。
- **预期亮点**： LangGraph checkpointer 能力从"演示"升级为"真能用"，是面试/答辩时最能体现工程深度的点。
- **开发难度**：**低**（核心只改 3 行，界面加一个列表）。

### 创意 2：AI 玩家的"记仇"记忆——发言引用机制
- **解决痛点**：AI 玩家只看最近 6 条消息（nodes.py:498），前面的发言说过什么它全忘了，盘问没有连续性，"你刚才不是说……吗？"这种剧本杀精髓玩法不存在。
- **实现思路**：在 state 里加 `memory: dict`（角色→记住的关键信息列表）。每个 AI 发言节点结束后，加一个轻量"记忆抽取"步骤（可用规则：提取被点名、被质疑的句子；进阶用一次小模型调用），下一轮该角色发言时 prompt 附上"你记得：…"。
- **预期亮点**：AI 会在第 5 轮突然引用你第 2 轮说过的话反将一军——拟人感质变，这是当前项目最缺的"智能体味道"。
- **开发难度**：**中**（规则版低，LLM 抽取版中）。

### 创意 3：DM 动态提示系统——卡关自适应
- **解决痛点**：用户是新手时，讨论几轮毫无头绪也只能硬聊；现有流程没有任何难度调节。
- **实现思路**：新增确定性检测节点（每 N 轮跑一次）：统计最近发言中"私密线索关键词"被提及的比例，若连续 2 轮低于阈值，判定"卡关"，触发 DM 插入一句定向暗示（从从未被提及的线索里挑一条，改写成模糊提示）。这是"确定性逻辑判断 + LLM 生成话术"分层思想的又一次复用。
- **预期亮点**：自适应难度是游戏 AI 的热门方向，且完全复用项目现有架构，不引入新依赖。
- **开发难度**：**中**。

### 创意 4：可观测性调试面板
- **解决痛点**：目前 thoughts（内心戏）、重试次数、每节点耗时全都没有出口，调 prompt 只能靠 print，出问题难定位。
- **实现思路**：两条路任选——① 零代码路线：设置 `LANGCHAIN_TRACING_V2=true` 接入 LangSmith 免费版，直接看每个节点的输入输出与耗时；② 自建路线：节点里用 `time.perf_counter()` 记录耗时写入 state 的 `metrics` 字段，app.py 加一个 `st.expander("🔧 调试面板")`，展示 thoughts 时间线、防泄露重试次数、各节点耗时柱状图（可用 st.bar_chart）。
- **预期亮点**：调试面板里看 AI 的内心戏和"说漏嘴被打回"的过程，本身就是极好的演示素材。
- **开发难度**：**低**（自建版约半天）。

### 创意 5：人物关系图可视化
- **解决痛点**：嫌疑人之间的关系、线索指向全靠玩家脑补，玩到后期容易乱。
- **实现思路**：剧本生成时让 LLM 多输出一个 `relations` 字段（如 `[{"from":"陆明轩","to":"沈碧如","rel":"夫妻","known_to":"公开"}]`），用 `pyvis`（纯前端 HTML，无需服务）渲染成交互关系图嵌在 Streamlit 侧边栏（`components.html`）；随讨论推进，把"已被公开提及的线索"对应的边高亮。
- **预期亮点**：从文字游戏升级为图文推理，视觉冲击力强，pyvis 学习成本很低。
- **开发难度**：**中**。

### 创意 6：LLM 评审员——剧本质量自动打分
- **解决痛点**：现在无法回答"这一局生成的剧本好不好"——线索是否自洽、凶手线索是否足够、有没有逻辑漏洞，全靠人肉体验。
- **实现思路**：新增 `eval_script.py`：生成剧本后，用另一次 LLM 调用扮演"剧本杀评审"，按清单打分（每人线索数量≥2？凶手有护身符？truth 与线索是否一致？forbidden 是否真的出现在 secret 里？），输出评分报告；跑 20 局统计平均分，作为你改 prompt 的量化依据。
- **预期亮点**："用 LLM 评估 LLM"是 Agent 领域主流做法（LLM-as-judge），有了它你的 prompt 迭代就从玄学变成科学。
- **开发难度**：**低**（一个独立脚本，不动主流程）。

---

## 五、优先级行动清单

### 🔴 立即修复（1~2 天，都是小改动大收益）
| # | 任务 | 位置 |
|---|---|---|
| 1 | 剧本流式生成加异常捕获，失败给出可重试的错误提示 | app.py 131~137 行 |
| 2 | 修复 JSON 解析失败时 think 内心戏泄露为公开发言 | nodes.py 524 行 |
| 3 | DM 开场 prompt 不再传入 secret/forbidden，只传名字 | nodes.py 451~456 行 |
| 4 | 补建 `.env.example` | 仓库根目录 |
| 5 | diag.py 密钥打印脱敏 | diag.py 17 行 |

### 🟡 短期优化（1~2 周，工程化补课）
| # | 任务 | 位置 |
|---|---|---|
| 6 | 四处字符串拼接改 list+join（O(n²)→O(n)） | nodes.py 192/325、app.py 50/134 |
| 7 | 抽取 `_current_speaker()` 消除发言者逻辑重复 | nodes.py 488/703 行 |
| 8 | 防泄露重试耗尽后加确定性脱敏兜底 | nodes.py 521~531 行 |
| 9 | 平票检测与处理 | nodes.py `tally_node` |
| 10 | 为 8 个纯函数写 pytest 测试（重点覆盖已踩过的坑） | 新建 tests/ |
| 11 | 拆分 nodes.py → names/prompts/validators/nodes | nodes.py |
| 12 | votes 加合并 reducer，简化 human_vote_node | game_state.py + nodes.py |
| 13 | 类型注解补全（GameState、list[str]）+ requirements 补 httpx + 修 test_api.py 旧路径注释 | 全仓库 |

### 🟢 长期规划（按兴趣选做，每个都是答辩亮点）
| # | 任务 | 对应创意 |
|---|---|---|
| 14 | SqliteSaver 持久化 + 断点续玩 + 历史对局列表 | 创意 1 |
| 15 | AI 玩家记忆机制（发言引用） | 创意 2 |
| 16 | ai_vote 喂私密线索 + asyncio 并行投票 | 问题 8 + 优化点 5 |
| 17 | 调试面板（thoughts/耗时/重试可视化） | 创意 4 |
| 18 | 卡关自适应提示 | 创意 3 |
| 19 | 人物关系图可视化 | 创意 5 |
| 20 | LLM 评审员剧本打分 | 创意 6 |

---

## 附：无需补充材料即可开工的说明

本次审查已覆盖仓库全部 Python 源码与配置。**唯一缺失的文件是 `.env.example`**（README 引用了但不存在），需要新建；另外 test_api.py 注释提到的 `campus-agent` 目录不在本仓库，应是旧项目残留，不影响审查结论。密钥文件 .env 未被 git 跟踪、.gitignore 配置正确，这一点做得很好。
