# 优化提示词（给你的大模型工具）

> 用法：把下面「提示词正文」整段复制给你的 AI 编程助手（Claude Code / Cursor / WorkBuddy 等），
> 它会基于项目现状帮你做优化。你可以先看「怎么用」，再把正文丢给它。

---

## 怎么用（给队友看的说明）

1. 打开项目目录（`Agent_Developing`），让 AI 助手能读到代码。
2. 复制下面的提示词正文，粘贴给 AI 助手。
3. 在「优化目标」那段里，**从列出的方向中选 1~2 个**（或写你自己的需求），再发给 AI。
4. 拿到 AI 的改动后，先 `python main.py` 或 `streamlit run app.py` 验证能跑通，再合并。

---

## 提示词正文（复制下面整段）

```
你是一位资深 LangGraph 和多智能体系统工程师。我现在有一个已经能跑通的项目「AI 剧本杀主持人」，需要你在【不破坏现有功能】的前提下做优化。

## 项目现状

这是一个基于 LangGraph 的多智能体剧本杀系统：真人玩家扮演一个嫌疑人，与多个 AI 角色（其他嫌疑人 + 主持人 DM）在共享状态上协作，完成「生成剧本 → 讨论 → 投票 → 揭晓真相」的完整游戏闭环。

技术栈：LangGraph（图编排 + interrupt 人在回路）、langchain-deepseek 调用 DeepSeek V4-Flash、Streamlit 做界面、Python 3.10。

## 项目文件（先读这些）

- `game_state.py`：共享状态 GameState（TypedDict, total=False），关键字段 theme / background_style / script / current_phase / phase_round / user_role / messages / thoughts / votes / vote_winner / vote_counts / clues_pool / distributed_clues
- `nodes.py`：节点函数——generate_script_node / dm_intro_node / ai_player_turn_node / human_turn_node / ai_vote_node / human_vote_node / tally_node / dm_reveal_node / route_speaker
- `graph.py`：图编排（条件边 + 循环 + checkpointer=MemorySaver）
- `app.py`：Streamlit 界面（st.session_state + interrupt 恢复）
- `main.py`：终端入口

## 核心机制（改之前必须先理解）

1. 图结构：generate_script → dm_intro → [human_turn / ai_player_turn 讨论循环] → ai_vote → human_vote → tally → dm_reveal
2. 条件边 route_speaker 根据 phase_round 决定下一个发言者（返回 human / ai / vote）
3. 人在回路：节点里 interrupt({...}) 暂停，入口用 Command(resume=用户输入) 恢复，检测 result["__interrupt__"][0].value
4. messages 用 operator.add 追加，元素是 {"speaker": str, "content": str}
5. AI 玩家是 think/speak 双通道，且有禁忌词（forbidden）防泄露重试
6. tally_node 是确定性节点（Counter 统计票数），不调 LLM

## 优化目标（选 1~2 个，或写你自己的）

【在这里填入你选的方向，例如：】
- 实现线索分发：让每个角色持有不同线索（clues_pool / distributed_clues 字段已预留）
- 增加用户选择角色：开局让用户从嫌疑人里选一个扮演
- 平票重投：tally_node 平票时重新投票而不是取第一个
- 流式输出：Streamlit 用 st.write_stream 流式显示 LLM 生成
- 对话记忆增强：让 AI 记住更长历史（当前只拼最近 6 条）
- 优化剧本质量：让线索更有误导性、投票更分散

## 硬约束（必须遵守）

1. 不破坏现有可运行状态：改完必须 python main.py 和 streamlit run app.py 都能跑通
2. 保持核心主流程：讨论循环 → 投票 → 揭晓 这条线不动
3. 密钥安全：DeepSeek key 在 .env，绝不下代码、不进 git
4. 模型名保持 deepseek-v4-flash，reasoning_effort="none" 必须保留
5. Python 3.10，代码配中文注释

## 输出要求

1. 给出修改的具体代码，标清文件路径和改动位置
2. 每个改动配中文注释，解释"为什么这么改"
3. 说明改动后如何测试
4. 如引入新依赖，列出来并说明用途
```

---

## 附：可选的优化方向清单（给队友参考）

| 优先级 | 方向 | 说明 |
|---|---|---|
| 高 | 线索分发（信息差） | `clues_pool`/`distributed_clues` 已预留，让角色持有不同线索 |
| 高 | 用户选择角色 | 当前固定第一个嫌疑人，可开局选择 |
| 高 | 平票重投 | `tally_node` 平票取第一个，可改重投 |
| 中 | 流式输出 | Streamlit 改 `st.write_stream` |
| 中 | 对话记忆增强 | AI 只拼最近 6 条，可引入总结记忆 |
| 中 | 剧本质量 | 线索指向性太强导致投票太一致，优化提示词 |
| 低 | 配置外置 | MAX_ROUNDS、temperature 等硬编码外置 |
| 低 | 自动化测试 | 目前无测试 |
