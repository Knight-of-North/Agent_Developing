# 上下文总结（AI 剧本杀主持人项目）

> 本文件是跨会话衔接的**第一入口**。新会话开头先读这里，再按需读 `.workbuddy/memory/` 下的工作日志链（最近优先）。

---

## 项目位置与运行

- **仓库**：`repository/Agent_Developing`（本文件所在目录）
- **环境**：conda env `py10`（Python 3.10，Anaconda，路径 `D:\03_Software\Developer_Tools\software\Anaconda\CondaEnvs\py10\python.exe`）
- **运行**：`streamlit run app.py`（Web 版推荐）/ `python main.py`（终端版）
- **API**：DeepSeek `deepseek-v4-flash`，`.env` 配置，余额约 ¥3.28（**每局硬约束**）

---

## 当前状态（2026-08-21 11:54，已全部提交）

| 项 | 状态 |
|---|---|
| pytest | **92 passed**（~4.7s） |
| 沉浸 UI | ✅ C-1~C-4 + G0 + **7 轮联调修复全部落地** |
| 关系图 | ✅ 死者 fixed 居中 + 嫌疑人互连（8-21 重构） |
| README | ✅ 重写为"暑假智能体开发风格" + 3 张截图入库 |
| git | ✅ **工作区干净**，最新 commit `7402ae0`「README次优化」 |
| 答辩亮点 | ✅ 已分析（四层：核心论点/技术硬核/工程素养/体验完整） |

---

## 技术栈速览

- **LangGraph 14 节点图**：`generate_script → distribute_clues → choose_role → dm_intro → self_intro →` 讨论循环（`ai_player_turn / human_turn / dm_midpoint`）`→ ai_vote → human_vote → tally → final_statement → dm_reveal`
- **核心机制**：think/speak 双通道、interrupt 人在回路、信息差（线索按 holder 分发）、防泄露校验、凶手狡辩策略池、轮换公平（读 messages 末位）
- **沉浸 UI**：暗色档案风（#141110 + 金 + 红）、雷雨氛围音（Web Audio 三层合成）、开场动画（打字机 + 红章）、阶段幕布 + 音效
- **关系图**：pyvis 0.3.2 + vis.js，死者 fixed 居中（mass=2 + x=0/y=0 + fixed），嫌疑人围绕 + 互相连线

---

## 8-20 联调修复清单（已落地，勿重复修）

1. **主持人旁白用「我」** → `nodes.py` `dm_intro_node` + `dm_reveal_node` 的 prompt 加【硬性人称约束】块
2. **chat_input 底部大片白** → CSS 新增 `[data-testid="stBottom"]` / `footer` / `stDecoration` 暗色覆盖
3. **选完角色 4 人只剩 2 个** → sidebar 把「嫌疑人总览」提到角色卡顶部（expander + markdown）
4. **开始页小字看不清** → 新增 `--murder-sans` 字体链 + muted/placeholder 提亮 + caption 14px
5. **`result=None` 崩溃**（生成中刷新）→ `interrupt_handler.get_interrupt` 加 dict 防御 + app.py 回滚输入页
6. **selectbox 白底** → baseweb `*` 通配 + `background-image: none` + `box-shadow: none` 兜底
7. **expander 内容区白底** → 同套方案（`[data-baseweb="accordion"] *` 穿透 + 内容 markdown 设色）

## 8-21 修复清单（已落地，勿重复修）

1. **expander header 白底** → 之前漏了原生 `<details><summary>` 和 `.streamlit-expanderHeader` 的 background 选择器，`app.py` `_THEME_CSS` 补上
2. **关系图"以死者为中心"重构** → 三层改动：
   - 渲染层 `visualization.py`：死者 `mass=2` + `x=0,y=0` + `fixed={"x":True,"y":True}` 钉死中心
   - 数据层 `prompts.py`：人物关系铁律加"嫌疑人之间也要有公开边"（不能只有嫌疑人→死者的放射线）
   - 校验 `nodes.py`：第 7 条校验，4 人以上局嫌疑人间公开边 ≥ ⌈N/2⌉（6 人局 ≥3）
3. **README 重写** → 228 行"暑假智能体开发风格"，新增效果展示（3 张图）+ 踩坑表 + 学习路径 + 作者署名；截图在 `docs/screenshots/`（01-start-page / 02-choose-role / 03-dm-opening）

---

## 关键约定（血泪坑）

1. 改 `nodes.py` / `prompts.py` / `app.py` 的 **Python 逻辑**后 Streamlit 必 **Ctrl+C 重启**；**纯 CSS/HTML 改动会热重载**
2. `st.components.v1.html()` **不支持 key 参数**；跨局重播靠 HTML 拼 `thread_id`（`data-tid`）
3. `set_options` 字符串内**不能写 `#` 注释**（`json.loads` 会崩）
4. LLM 爱偷懒只讲故事不填 JSON → `_check_script_consistency` 已扩到 7 条硬检测 + 重试反馈
5. 手机热点 IP 风控会报 `APIConnectionError`（非代码 bug）
6. 铁律：**名单外角色不能登场**（`relations` / `private_clues` 会崩）
7. **baseweb 组件暗色化模板**（Select/Accordion/Input 通用）：外层 + `*` 通配 + `background-image: none !important` + `box-shadow: none !important` + 内容 markdown 设色 + transparent
8. **started 与 result 是独立 session_state 键**：生成中刷新会中断，必须防御
9. **pyvis 0.3.2 的 `mass` / `x` / `y` / `fixed` 字段都能序列化**，但**验证时要确认字段真的进了 HTML**（`build_relations_html` 输出里搜 `"mass"` 等）
10. **关系图渲染层要克制**：`centralGravity=0.3` + `mass=3` 会让节点挤成"紧张主题"，飞哥明确反感——数据合理是第一性原理，渲染层只做兜底

---

## 待办（开学报告前）

1. **答辩准备**（可选）：答辩亮点已分析过，可整理成一页 PPT 大纲或 3 分钟开场陈述稿
2. **演示前**：`python diag.py` 测连通，现场 30 秒跑一局最有力
3. git 已全部提交（工作区干净），报告前如需改动记得打快照

---

## 遗留（开学后再说）

- 图流程 mock 冒烟测试（fake LLM 跑整局）——测试唯一盲区
- 历史摘要控 token（¥3.28 硬约束未解）
- beautiCode bug 报告（.md 回给开发者，次要事务）

---

**建议新会话第一句话**：「继续剧本杀项目，先跑 pytest 确认状态（预期 92 passed），然后按需要做答辩准备或继续联调。」
