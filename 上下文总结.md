# 上下文总结（AI 剧本杀主持人项目）

> 本文件是跨会话衔接的**第一入口**。新会话开头先读这里，再按需读 `.workbuddy/memory/` 下的工作日志链（最近优先）。

---

## 项目位置与运行

- **仓库**：`repository/Agent_Developing`（本文件所在目录）
- **环境**：conda env `py10`（Python 3.10，Anaconda，路径 `D:\03_Software\Developer_Tools\software\Anaconda\CondaEnvs\py10\python.exe`）
- **运行**：`streamlit run app.py`（Web 版推荐）/ `python main.py`（终端版）
- **API**：DeepSeek `deepseek-v4-flash`，`.env` 配置，余额约 ¥3.28（**每局硬约束**）

---

## 当前状态（2026-08-20 16:38）

| 项 | 状态 |
|---|---|
| pytest | **91 passed**（~4.4s） |
| 全量编译 | ✅ 13 个 py 全绿 |
| 沉浸 UI | ✅ C-1~C-4 + G0 + **6 轮联调修复全部落地**，飞哥正在实测中 |
| git | ✅ **已 commit** `[ShuFei 7348a27]`「沉浸UI+终审修复+联调修复」（4 files, +629/-25） |
| 服务器 | 飞哥本地跑 `localhost:8501`（之前后台 8601 已停） |
| docs/ | 已归档 6 份报告（审查/评估/调研/终审） |

---

## 技术栈速览

- **LangGraph 14 节点图**：`generate_script → distribute_clues → choose_role → dm_intro → self_intro →` 讨论循环（`ai_player_turn / human_turn / dm_midpoint`）`→ ai_vote → human_vote → tally → final_statement → dm_reveal`
- **核心机制**：think/speak 双通道、interrupt 人在回路、信息差（线索按 holder 分发）、防泄露校验、凶手狡辩策略池、轮换公平（读 messages 末位）
- **沉浸 UI**：暗色档案风（#141110 + 金 + 红）、雷雨氛围音（Web Audio 三层合成）、开场动画（打字机 + 红章）、阶段幕布 + 音效

---

## 8-20 联调修复清单（今天下午已落地，勿重复修）

1. **主持人旁白用「我」** → `nodes.py` `dm_intro_node` + `dm_reveal_node` 的 prompt 加【硬性人称约束】块（禁止第一人称，✅/❌示例）
2. **chat_input 底部大片白** → CSS 新增 `[data-testid="stBottom"]` / `footer` / `stDecoration` 暗色覆盖
3. **选完角色 4 人只剩 2 个** → sidebar 把「嫌疑人总览」提到角色卡顶部（expander + markdown，替代不稳定的 `st.write`）
4. **开始页小字看不清** → 新增 `--murder-sans` 字体链，`--murder-muted: #a2947a→#D4C4A0`，placeholder `#6f6550→#9a8e75`，caption 强制 14px+500；标题/按钮保留 serif
5. **`result=None` 崩溃**（生成中刷新页面）→ `interrupt_handler.get_interrupt` 加 `not isinstance(result, dict)` 防御；`app.py` else 分支 result=None 时回滚输入页 + `interrupt_notice` 一次性提示
6. **selectbox 白底** → baseweb `*` 通配 + `background-image: none` + `box-shadow: none` 兜底（收起态 + 虚拟 dropdown 都覆盖）
7. **expander 内容区白底** → 同套方案（`[data-baseweb="accordion"] *` 穿透 + 内容 markdown 强制米色 + transparent 背景）

---

## 关键约定（血泪坑）

1. 改 `nodes.py` / `prompts.py` / `app.py` 的 **Python 逻辑**后 Streamlit 必 **Ctrl+C 重启**（热重载坑）；**纯 CSS/HTML 改动会热重载**，不用重启
2. `st.components.v1.html()` **不支持 key 参数**；跨局重播靠 HTML 拼 `thread_id`（`data-tid` 属性）
3. `set_options` 字符串内**不能写 `#` 注释**（`json.loads` 会崩）
4. LLM 爱偷懒只讲故事不填 JSON 字段 → `_check_script_consistency` 已扩到 5 条硬检测 + 重试反馈
5. 手机热点 IP 风控会报 `APIConnectionError`（非代码 bug）
6. 铁律：**名单外角色不能登场**（`relations` / `private_clues` 会崩）
7. **baseweb 组件暗色化模板**（Streamlit 1.61，Select/Accordion/Input 通用）：外层容器 + `*` 通配穿透 + `background-image: none !important` + `box-shadow: none !important` 兜底 + 内容 markdown 文字显式设色 + `background-color: transparent` 透传
8. **started 与 result 是独立 session_state 键**：生成中刷新页面会中断生成循环，result 永远到不了位 → 必须防御

---

## 待办（开学报告前）

1. **继续联调沉浸 UI**（飞哥正在测，可能还有新反馈——大概率是其他 baseweb 组件白底，套模板修即可）
2. 可选：README 截图占位（`screenshots/`）、演示前 `python diag.py` 测连通
3. 之前 git 已 commit，**新修改（今天 16:16 之后：selectbox/expander 白底修复等 CSS）未提交**——报告前再打一次快照

---

## 遗留（开学后再说）

- 图流程 mock 冒烟测试（fake LLM 跑整局）——测试唯一盲区
- 历史摘要控 token（¥3.28 硬约束未解）
- beautiCode bug 报告（.md 回给开发者，次要事务）

---

**建议新会话第一句话**：「继续剧本杀项目，先跑 pytest 确认状态，然后继续沉浸 UI 联调。」
