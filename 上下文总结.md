# 项目上下文总结（用于新对话无缝衔接）

> 用法：新开对话后，把本文件内容完整贴给 AI，即可继承全部上下文。

---

## 一、项目是什么

**AI 剧本杀主持人** —— 基于 LangGraph 的多智能体协作系统。输入主题 + 背景风格，AI 自动生成剧本、主持流程、模拟 AI 玩家发言，用户扮演一个嫌疑人参与讨论并投票，最后 DM 揭晓真相。

完整闭环：

```
输入主题/背景 → 生成剧本 → DM 开场 → 用户与 AI 轮流发言(循环) → 投票 → 统计票数 → DM 揭晓真相+对比投票
```

## 二、项目位置

```
D:\01_Documents\Learning\class\spring_semester_of_freshmen_year\agent_developing\repository\Agent_Developing\
```

## 三、技术栈

| 项 | 值 |
|---|---|
| Python | 3.10（conda 环境名 `py10`） |
| LangGraph | 1.2.11 |
| LangChain | langchain-core 1.x + langchain-deepseek 1.1.0 |
| 模型 | DeepSeek **V4-Flash**（关键：`deepseek-chat` 已于 2026-07-24 退役，不可再用） |
| GUI | Streamlit（`app.py`） |
| pip 镜像 | 清华源（已全局配置） |

激活环境：`conda activate py10`

## 四、文件结构

| 文件 | 作用 |
|---|---|
| `game_state.py` | `GameState`（TypedDict，共享状态） |
| `nodes.py` | 所有节点函数 + `llm` 模型配置 + `_parse_json` 解析 |
| `graph.py` | `StateGraph` 编排 + `build_graph()` |
| `main.py` | 终端版入口（含 interrupt 恢复循环） |
| `app.py` | Streamlit 网页版（`@st.cache_resource` 缓存图） |
| `diagnose.py` / `diag.py` | 诊断脚本（模型/网络排查） |
| `README.md` / `开发总结.md` | 交付文档 |
| `交接说明.md` / `优化提示词.md` | 已改为自用（开发手册 / 自用 AI 辅助提示词） |
| `requirements.txt` / `.env.example` | 依赖 + 配置模板 |
| `.env` | 真实 key（**不入 git**） |

## 五、GameState 字段（精确）

`game_state.py`，`TypedDict, total=False`：

```python
theme: str                 # 主题
background_style: str      # 背景风格
background_story: str      # 用户自定义的剧情背景（可选，优先级高于 theme + background_style）
custom_names: list         # 用户自定义的嫌疑人名字（可选，优先于随机抽名）
script: dict               # 剧本 {background, suspects, public_clues, private_clues, truth}
current_phase: str         # generate -> intro -> discuss -> vote -> reveal
phase_round: int           # 讨论轮次
user_role: str             # 用户扮演的角色（choose_role_node 让用户从名单里选）
messages: Annotated[list, operator.add]   # 元素 {"speaker","content"}
thoughts: Annotated[list, operator.add]   # AI 内心戏(think)
players: dict              # {玩家名: 角色名}
ai_players: list           # AI 玩家
clues_pool: list           # 线索池（全部线索清单：公开 + 私密，distribute_clues_node 生成）
distributed_clues: dict    # 已分配线索 {角色名: [私密线索...]}（distribute_clues_node 生成）
votes: dict                # {投票者: 被投者}
vote_winner: str           # 得票最多者
vote_counts: dict          # {嫌疑人: 票数}
```

## 六、图结构 + 节点清单

```
START → generate_script → distribute_clues → choose_role → dm_intro ─┐
                                                                      │（条件边 route_speaker）
        ┌─────────────────────────────────────────────────────────────┘
        ▼
  human_turn(interrupt 等用户) ←──┐
  ai_player_turn(AI 发言)       ←──┼── 循环
        │                          │
        └── 轮满 → ai_vote → human_vote → tally → dm_reveal → END
```

节点（均在 `nodes.py`）：`generate_script_node`、`distribute_clues_node`、`choose_role_node`、`dm_intro_node`、`ai_player_turn_node`、`human_turn_node`、`ai_vote_node`、`human_vote_node`、`tally_node`、`dm_reveal_node`，路由函数 `route_speaker`。

三个 interrupt 暂停点：`choose_role_node`（选角色）、`human_turn_node`（发言）、`human_vote_node`（投票）。

图带 `checkpointer=MemorySaver()`，这是 interrupt 能"暂停并记住进度"的前提。

## 七、关键机制 / 踩过的坑（重要！）

1. **模型名**：必须用 `deepseek-v4-flash`。`deepseek-chat` 已退役。
2. **思考模式**：V4 默认开 thinking，会把答案塞进 `reasoning_content`，导致 `content` 空。已用 `reasoning_effort="none"` 关闭。关闭后 `temperature` 才生效。
3. **messages 用 `operator.add`**（不是 `add_messages`），因为元素是自定义 dict 而非 LangChain message 对象。
4. **think/speak 双通道**：AI 玩家输出 `{think, speak}`，`think` 存 `thoughts`（不公开），`speak` 存 `messages`（公开）。
5. **确定性节点**：`tally_node` 用 `Counter` 统计票数，不调 LLM（LLM 数票会出错）。
6. **interrupt 人在回路**：`interrupt()` 暂停，`Command(resume=...)` 恢复；恢复时节点会**从头重跑**，所以 `interrupt()` 之前的代码必须幂等。
7. **防跑飞**：`generate_script_node` 让 LLM 给每个角色标 `forbidden`（禁忌词），`ai_player_turn_node` 检查发言是否泄露禁忌词，泄露则打回重说（最多 3 次）。
8. **llm 配置**（`nodes.py`）：`temperature=0.8`、`max_tokens=4096`、`timeout=120`、`max_retries=5`、`http_client=httpx.Client(trust_env=False)`（强制不走代理直连）。
9. **让 LLM 如实转述的数据必须完整传给它**：DM 揭晓时若只给票数摘要，模型会脑补"谁投了谁"甚至编造死人投票——必须把完整投票明细传入提示词。
10. **网络问题（已定位）**：之前偶发 `APIConnectionError`/`SSL EOF`，根因是手机热点 IP 段被 DeepSeek 临时风控，账户后台正常（余额 ¥3.28）。已通过 `max_retries=5` + 换 IP（飞行模式重拨）缓解。**非代码 bug**。
11. **信息差三层结构（Phase 4.5）**：① 编剧层（LLM 生成 `private_clues` 时定好每条 `holder`，强制"凶手拿护身符、侦探拿指向真凶的拼图"）；② 分发层（`distribute_clues_node` 纯 Python 按 holder 归位，holder 写错走"负载均衡兜底"，不调 LLM）；③ 可见性层（DM 只公布 public_clues，AI 发言只注入自己的私密线索，用户角色卡只显示自己的）。`_fallback_clues` 兜底旧格式 `clues` 字段。
12. **随机性交给代码、创作交给 LLM（Phase 5.5）**：嫌疑人名字不再让 LLM 编（LLM 的"随机"趋同、翻来覆去就那几个高频名），改成 `_pick_suspect_names` 用 `random.sample` 从按背景风格分组的名字池（`_NAME_POOLS`，6 组）里无放回抽 4~6 个名字，再通过 prompt 强约束 + `_enforce_names` 兜底强制 LLM 用这些名字编故事。名字是 choose_role/route_speaker/distribute 的"主键"，必须由代码掌控。
13. **流式输出 = 生成器 generator（Phase 6）**：`_char_stream` 是生成器函数（函数体有 `yield`），调用返回 generator 对象不执行函数体，`next()` 才逐个执行到 yield。`st.write_stream(_char_stream(text))` 内部反复 next 并刷新界面，实现打字机。这是**前端伪流式**——后端 LLM 仍一次性生成；真流式（`llm.stream()`）见第 14 条。
14. **生成剧本真流式（Phase 6.5）**：`generate_script_stream` 用 `llm.stream()` 逐 token yield，前端边生成边显示，摆脱"盯 spinner 干等"。关键技巧：生成器既 `yield` 中间 token、又 `return` 最终 script（for 循环拿不到 return 值，要手动 `next()` 从 `StopIteration.value` 取）。`generate_script_node` 改为"state 已有 script 就跳过、否则 invoke 兜底"，所以前端预生成 script 传图后，节点不重复生成。
15. **发言串成投票 bug（已修，两层根因）**：① Streamlit `st.chat_input` 在 `st.rerun()` 后值会残留（GitHub issue #7629），发言文本被下一轮的投票输入框读到当成投票——用显式不同 key（`speak_input`/`vote_input`）+ 前端校验 + `human_vote_node` 兜底校验（resume 值非嫌疑人名字则丢弃）三层防御。② 讨论轮数写死 `MAX_ROUNDS=6`，嫌疑人随机 4~6 人时用户可能只发言 1 次就被推进投票——改成动态 `len(suspects) × ROUNDS_PER_PLAYER`（`ROUNDS_PER_PLAYER=2`，每人至少发言 2 次）。
16. **secret 第一人称化（代入感）**：LLM 写 secret 时偷懒会用「他/她」开头（"他暗恋宋知夏"），破坏玩家扮演该角色的代入感。修法：prompt 强约束"必须用「我」开头"+ `_normalize_secret_first_person` 兜底替换（开头的"她/他/她的/他的"→"我/我的"；中间的人称指别人，不替换）。
17. **发言环节真流式（Phase 7）**：三个发言节点（`dm_intro`/`ai_player_turn`/`dm_reveal`）改用 `_stream_full_text`（内部 `llm.stream()` 累积），前端 `_run_stream` 用 `graph.stream(stream_mode=["messages","updates"], version="v2")` 逐 token 实时显示。关键：`stream_mode="messages"` 靠 LangChain callback 系统捕获 token，**节点必须用 `llm.stream()` 而非 `llm.invoke()`**；interrupt 用 `graph.get_state(config).values` 拿完整 state。打字机 `_char_stream` 已删除（真流式取代它）。
18. **自定义背景：素材种子 > 强制照抄（prompt 措辞）**：用户给自定义背景时，命令式措辞"务必严格基于它展开、不要偏离"会让 LLM 把原文照抄进 `background` 字段，生硬没代入感。改成"当作素材种子，用你自己的编剧语言扩写演化、补时间地点人物关系动机冲突，不要照抄原文"；`background` 字段描述改为"扩写后的完整案件背景"。DM 开场也改为"讲故事般娓娓道来，不要照念"。核心经验：**给 LLM 素材要引导它"消化再创作"，而不是"原样搬运"**。

## 八、当前进度

- ✅ Phase 1：线性流程（剧本→DM 开场→揭晓）
- ✅ Phase 2：多智能体循环讨论 + think/speak 双通道
- ✅ Phase 3：投票 + tally 确定性统计
- ✅ Phase 3.5：用户参与（interrupt 人在回路 + MemorySaver）
- ✅ Phase 4：防跑飞校验 + README/开发总结
- ✅ GUI：Streamlit 网页版
- ✅ 背景自定义：`theme` + `background_style`（六选一：自由发挥/民国豪门/校园怪谈/古风仙侠/现代都市/科幻末世）
- ✅ Phase 4.5：线索分发（信息差）。剧本生成时输出 `public_clues`（公开）+ `private_clues`（每条带 `holder` 归属），`distribute_clues_node` 确定性分发到 `distributed_clues`，DM 只公布公共线索，AI/用户只看得见自己的私密线索。凶手拿护身符、侦探拿指向真凶的拼图。
- ✅ Phase 5：用户选择角色。新增 `choose_role_node`（interrupt 暂停），开局让用户从嫌疑人名单里自由挑选，不再固定扮演第一个嫌疑人。选完角色才打印角色卡。
- ✅ Phase 5.5：嫌疑人名字随机性。`_pick_suspect_names` 按背景风格从名字池随机抽 4~6 个名字（人数也随机），`_enforce_names` 兜底强制剧本用这些名字，解决"每局名字大差不差"的问题。
- ✅ Phase 6：流式输出。`app.py` 用 `_char_stream` 生成器 + `st.write_stream` 做打字机效果，最后一条 AI 消息逐字输出，历史消息直接渲染。
- ✅ Phase 6.5：生成剧本真流式。`generate_script_stream` 用 `llm.stream()` 边生成边显示，`generate_script_node` 支持"已有 script 跳过"，前端预生成后传图。
- ✅ Bug 修复：发言串成投票。`st.chat_input` 值残留用显式 key + 校验 + 节点兜底三层防御；讨论轮数动态化（每人至少发言 2 次）。
- ✅ 自定义剧情背景。新增 `background_story` 字段，用户可写一段具体背景剧情（人物关系/事件起因/世界观），LLM 把它当素材扩写演化，而非照抄（措辞优化过，避免生硬）。
- ✅ Secret 第一人称化。prompt 强约束 + `_normalize_secret_first_person` 兜底，所有嫌疑人的秘密都以「我」开头，玩家扮演时更有代入感。
- ✅ 线索复盘（信息差闭环）。`dm_reveal_node` 注入"私密线索总账"+"公开讨论记录"，DM 揭晓时点出哪些线索从头到尾没被挖出来，补上信息差玩法的最后一块拼图。
- ✅ Phase 7：发言环节真流式。三个发言节点改 `llm.stream()`，`app.py` 用 `graph.stream(stream_mode="messages", version="v2")` 逐 token 实时显示，打字机 `_char_stream` 删除。
- ✅ 自定义嫌疑人名字。新增 `custom_names` 字段 + `_parse_names` 解析，用户可二选一（随机生成 / 自定义名字），自定义优先（≥3 个才采用，可用朋友/同学名增强代入感）。

**整个游戏已"完整可玩"**（终端 + 网页双入口）。

## 九、下一步（已排序，一次做一项）

1. **对话记忆增强** —— 讨论轮次多时，历史截断（`[-6:]`/`[-10:]`）会丢早期关键信息，可改成更聪明的摘要/检索。
2. 剧本质量优化 / 配置外置 / 测试。

## 十、开发风格约定

- 边做边学：每写一段代码就搞懂一个底层概念，不"先读完再动手"。
- 你写骨架/逻辑，AI 填代码；跑不通先自己 debug 再问 AI。
- 中文注释讲原理，关键概念（State/Node/Edge/reducer/条件边/interrupt）逐个讲透。
- 变量名用英文，密钥走 `.env` 不进 git。
